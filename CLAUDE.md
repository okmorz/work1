# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンスを提供します。

## プロジェクト概要

（プロジェクトの目的・概要をここに記載してください）

## Git運用ルール

- **コードを変更するたびに、必ず変更内容をコミットし、GitHubへプッシュすること。** 作業を溜め込まず、変更単位ごとに都度コミット・プッシュを行う。
- コミットメッセージは変更内容が分かるように簡潔に記載する。
- プッシュ前に `git status` / `git diff` で差分を確認し、意図しないファイル（機密情報や不要なファイル）が含まれていないか確認する。
- force push（`--push --force` 等)、`git reset --hard`、履歴の書き換えなど破壊的な操作は行わない。
- 作業ブランチの運用方針（例: main直push可否、PR経由か等）は必要に応じて追記する。

## よく使うコマンド

（ビルド・テスト・Lint等のコマンドをここに追記してください）

## アーキテクチャ

（ディレクトリ構成や設計上の注意点をここに追記してください）
